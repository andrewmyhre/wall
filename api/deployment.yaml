apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: api
  namespace: wall
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api
    matchExpressions:
      - {key: app, operator: In, values: [api]}
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: andrewmyhre/api
        ports:
        - containerPort: 80
---
kind: Service
apiVersion: v1
metadata:
  name: api
spec:
  type: LoadBalancer
  selector:
    app: api
  ports:
  - port: 80
    targetPort: 8000
